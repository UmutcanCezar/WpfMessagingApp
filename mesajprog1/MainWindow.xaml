<Window x:Class="mesajprog1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vms="clr-namespace:mesajprog1.ViewModel"
        xmlns:views="clr-namespace:mesajprog1.View"
        mc:Ignorable="d"        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        Title="LoginView" Height="768" Width="1250"
        >
    <Window.DataContext>
        <vms:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Border Grid.ColumnSpan="2"
                Background="#252525"
                MouseDown="Border_MouseDown">
            <Grid HorizontalAlignment="Stretch">
                <Label Content="Demirbaş"
                       Foreground="Gray"
                       FontWeight="SemiBold"
                       Margin="10 0 0 0"/>
                <StackPanel HorizontalAlignment="Right"
                            Orientation="Horizontal" 
                            Margin="0 0 5 0">

                    <Button Width="20" Height="20" 
                            Content="_"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="gray"
                            FontWeight="Bold"
                            Margin="0 0 0 3"
                            Command="{Binding MinimizeCommand}"
                            />

                    <Button Width="20" Height="20" 
                            Content="☐"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="gray"
                            FontWeight="Bold"
                            Command="{Binding WindowStateCommand}"
                            />

                    <Button Width="20" Height="20" 
                            Content="✖"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="gray"
                            FontWeight="Bold" 
                            Command="{Binding CloseCommand}"
                            />


                </StackPanel>

            </Grid>

        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>
    </Grid>
</Window>

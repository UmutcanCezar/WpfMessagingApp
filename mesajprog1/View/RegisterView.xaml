<UserControl x:Class="mesajprog1.View.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vms="clr-namespace:mesajprog1.ViewModel"
             xmlns:local="clr-namespace:mesajprog1.View"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1250">
    <Border>

        <Border.Background>
            <LinearGradientBrush>
                <GradientStop Color="#a9a9a9" Offset="0"/>
                <GradientStop Color="#ffffff" Offset="0.75"/>

            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="450"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="500"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>







            <Grid Grid.Row="1" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Center">

                    <TextBox Width="250" HorizontalAlignment="Left" Margin="0 5" Padding="2" Text="{Binding Username,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Username:"
                             materialDesign:HintAssist.Background="Transparent"/>
                    <TextBox Width="250" HorizontalAlignment="Left" Padding="2"  Margin="0 5" Text="{Binding Email,UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource MaterialDesignFloatingHintTextBox}" materialDesign:HintAssist.Hint="Email:"
                             materialDesign:HintAssist.Background="Transparent"/>
                    <PasswordBox Width="250" HorizontalAlignment="Left" Padding="2" Margin="0 5" vms:PasswordBoxAssistant.BoundPassword="{Binding Password,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource MaterialDesignFloatingHintRevealPasswordBox}" materialDesign:HintAssist.Hint="Reset Password:" />
                    <PasswordBox Width="250" HorizontalAlignment="Left" Padding="2"  Margin="0 5" vms:PasswordBoxAssistant.BoundPassword="{Binding ResetPassword,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource MaterialDesignFloatingHintRevealPasswordBox}" materialDesign:HintAssist.Hint="Reset Password:"  />

                </StackPanel>
                <Grid Grid.Row="2" Height="50" VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Register" Width="120" HorizontalAlignment="Right" Command="{Binding RegisterCommand}"
                            Style="{StaticResource MaterialDesignFlatDarkBgButton}" 
                            ToolTip="MaterialDesignFlatSecondaryDarkBgButton"/>
                    <Button Grid.Column="2" Content="Back" Width="120" HorizontalAlignment="left" Command="{Binding BackToLoginCommand}"
                            Style="{StaticResource MaterialDesignFlatMidBgButton}" 
                            ToolTip="MaterialDesignFlatSecondaryDarkBgButton"/>

                </Grid>
            </Grid>
        </Grid>


    </Border>
</UserControl>
